fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
openai>=1.50.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
httpx>=0.27.0
python-multipart>=0.0.9
playwright>=1.45.0
numpy>=1.26.0
pillow>=10.0.0

# dictation. faster-whisper downloads its own model on first use; the nvidia
# wheels are what a GPU needs, and are optional -- without them it runs on the
# CPU with int8, which is fine for a small model. ctranslate2 links against
# CUDA 12, so these are the cu12 wheels even on a machine with a newer CUDA.
faster-whisper>=1.1.0
nvidia-cublas-cu12; platform_system == "Linux" and platform_machine == "x86_64"
nvidia-cudnn-cu12>=9,<10; platform_system == "Linux" and platform_machine == "x86_64"

# in-app editor formatting
black>=24.0.0

# dev
pytest>=8.0.0
pytest-asyncio>=0.24.0
anthropic>=0.120
