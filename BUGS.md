## Known bugs

(None currently tracked. The per-prompt tool save bug was fixed — dropdown now navigates on change, and tool checkboxes were removed in favor of system prompt text driving tool usage.)
